import 'package:flutter/material.dart';

class LivePriceUpdates extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Live Feed Price Updates'));
  }
}
